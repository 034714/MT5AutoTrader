# MT5AutoTrader 环境变量模板
# 复制本文件为 .env 后填写。
#
# 说明：本软件不调用 mt5.login()，MT5 连接依赖“终端已打开并已登录”。
# 下面三项仅用于将来需要程序化登录时预留，留空不影响使用。
MT5_LOGIN=
MT5_PASSWORD=
MT5_SERVER=

# 训练/回测数据目录（也可在网页「风控参数」或 trader_config.json 里改 kline_cache_dir）
KLINE_CACHE_DIR=D:\K线数据
